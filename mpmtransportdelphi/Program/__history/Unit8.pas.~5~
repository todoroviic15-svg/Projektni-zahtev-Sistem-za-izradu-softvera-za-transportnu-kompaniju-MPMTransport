unit Unit8;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, FMX.StdCtrls,
  FMX.DateTimeCtrls, FMX.Controls.Presentation, FMX.Edit, FMX.Objects , Caption;

type
  TIsporukaStartStop = class(TForm)
    Y: TPanel;
    btnStart: TButton;
    TimeEdit1: TTimeEdit;
    Z: TLabel;
    Label2: TLabel;
    tbxPocetnaLokacija: TEdit;
    Label1: TLabel;
    txbPocetnoGorivo: TEdit;
    Label3: TLabel;
    Panel1: TPanel;
    btnStop: TButton;
    Timer1: TTimer;
    lblTajmer: TLabel;
    procedure Timer1Timer(Sender: TObject);
    procedure btnStartClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  IsporukaStartStop: TIsporukaStartStop;

implementation

{$R *.fmx}

procedure TIsporukaStartStop.btnStartClick(Sender: TObject);
begin
          Timer1.Enabled := true ;
end;

procedure TIsporukaStartStop.Timer1Timer(Sender: TObject);
begin
          lblTajmer.Text := IntToStr(StrToIntDef(lblTajmer.Text, 0) + 1);
end;

end.
