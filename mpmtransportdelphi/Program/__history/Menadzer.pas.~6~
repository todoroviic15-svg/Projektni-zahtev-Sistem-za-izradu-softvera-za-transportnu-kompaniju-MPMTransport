unit Menadzer;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs,
  FMX.Controls.Presentation, FMX.StdCtrls, FMX.Objects, FMX.Maps, UnosPodataka,
  FMX.ListBox, System.Rtti, FMX.Grid.Style, FMX.ScrollBox, FMX.Grid, Data.DB,
  Data.Win.ADODB;

type
  TForm3 = class(TForm)
    MapView1: TMapView;
    PrijavaBtn: TButton;
    Button1: TButton;
    Button2: TButton;
    Button3: TButton;
    btnChat: TButton;
    Text1: TText;
    SpeedButton1: TSpeedButton;
    ComboKorisnici: TComboBox;
    ADOQuery1: TADOQuery;
    ADOConnection1: TADOConnection;
    DugmeUdji: TButton;
    procedure SpeedButton1Click(Sender: TObject);
    procedure FormCreate(Sender: TObject);
    procedure DugmeUdjiClick(Sender: TObject);
  private
    procedure UcitajKorisnike;
  public
    { Public declarations }
  end;

var
  Form3: TForm3;

implementation
uses Vozac;

{$R *.fmx}
procedure TForm3.UcitajKorisnike;
var
  userID: Integer;
begin
  if not ADOConnection1.Connected then
    ADOConnection1.Connected := True;

  if ADOQuery1.Active then
    ADOQuery1.Close;


  ADOQuery1.SQL.Text := 'SELECT id, korisnickoime FROM users';
  ADOQuery1.Open;


  ComboKorisnici.Clear;

  // Prolazak kroz rezultate
  while not ADOQuery1.eof do
  begin
    // Hvatanje id
    userID := ADOQuery1.FieldByName('id').AsInteger;

    // Dodavanje u combo
    ComboKorisnici.Items.AddObject(
      ADOQuery1.FieldByName('korisnickoime').AsString,
      TObject(userID)
    );

    ADOQuery1.Next;
  end;
end;

procedure TForm3.DugmeUdjiClick(Sender: TObject);
var
  userID: Integer;
begin
  if ComboKorisnici.ItemIndex = -1 then Exit;

  userID := Integer(ComboKorisnici.Items.Objects[ComboKorisnici.ItemIndex]);
  Form7.PrijavljeniUserID := userID;
  Form7.Show;

  end;

procedure TForm3.FormCreate(Sender: TObject);
begin
ADOConnection1.ConnectionString :=
    'Provider=Microsoft.ACE.OLEDB.12.0;' +
    'Data Source=' + ExtractFilePath(ParamStr(0)) + 'transport1.accdb;';
  ADOConnection1.LoginPrompt := False;
  ADOConnection1.Connected := True;
UcitajKorisnike;
end;

procedure TForm3.SpeedButton1Click(Sender: TObject);
begin
Form2.Show;
Close;
end;

end.
