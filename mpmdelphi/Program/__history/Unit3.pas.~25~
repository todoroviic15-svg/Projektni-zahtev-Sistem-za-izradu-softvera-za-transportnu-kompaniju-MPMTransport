unit Unit3;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, Unit2, FMX.Maps,
  FMX.WebBrowser, FMX.Objects;

type
  TForm3 = class(TForm)
    WebBrowser1: TWebBrowser;
    Image1: TImage;
    procedure FormCreate(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form3: TForm3;


implementation

{$R *.fmx}

procedure TForm3.FormCreate(Sender: TObject);
var
  slikaPutanja: string;
begin
  // Precizna putanja do slike na računaru
  slikaPutanja := 'C:\Users\Mihajlo\Pictures\moja_slika.jpg';

  // Provera da fajl postoji
  if TFile.Exists(slikaPutanja) then
    Image1.Bitmap.LoadFromFile(slikaPutanja)
  else
    ShowMessage('Fajl ne postoji: ' + slikaPutanja);
end;

end.
